machine:
  services:
    - docker

dependencies:
  pre:
    - git clone git@github.com:jojomi/docker-hugo.git && cd docker-hugo
    - docker build -t jojomi/hugo:latest .

  override:
    - docker info

test:
  override:
    - docker run jojomi/hugo hugo version
